# Cria + Vercel AI SDK Example

This example shows how to use Cria with the Vercel AI SDK to build token-budget-aware prompts for OpenAI.

## Setup

```bash
# Install dependencies
pnpm install

```

## Run

Assumes `OPENAI_API_KEY` is set in your current env.

```bash
pnpm start
```


