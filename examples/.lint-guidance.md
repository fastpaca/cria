# Examples Guidance

- Prefer the DSL (`cria.prompt()`) in examples.
- JSX is optional and lives under `examples/jsx/`; only use it when demonstrating JSX explicitly.
- Keep examples minimal and runnable; avoid extra dependencies.
