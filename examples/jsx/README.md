# JSX Examples

JSX usage is optional. These TSX files show how to use the JSX entry at `@fastpaca/cria/jsx` for teams that prefer TSX syntax. The DSL remains the primary API.

If you prefer the DSL, use the examples in the integration folders (e.g., `examples/openai-chat-completions/main.ts`).
