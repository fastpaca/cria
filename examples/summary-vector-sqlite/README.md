# Summary + Vector with SQLite

Combine SQLite summaries and vector search in one libSQL file.

## Prerequisites

- `OPENAI_API_KEY`

Optional: set `RESET_SUMMARY=1` to clear only the summary cache.

## Run

```bash
pnpm install
pnpm start
```
