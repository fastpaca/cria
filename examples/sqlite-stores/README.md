# SQLite stores

Use SQLite KV store and sqlite-vec vector store together.

## Prerequisites

- `OPENAI_API_KEY`
- `SQLITE_VEC_PATH` (path to the sqlite-vec `vec0` extension)

## Run

```bash
pnpm install
pnpm start
```
