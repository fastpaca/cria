# Cria + OpenAI Chat Completions Example

This example shows how to use Cria with the OpenAI Chat Completions API.

## Setup

```bash
pnpm install
```

## Run

Assumes `OPENAI_API_KEY` is set in your environment.

```bash
pnpm start
```
