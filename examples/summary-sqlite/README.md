# Summary with SQLite

Progressive conversation summarization with a local SQLite-backed cache.

## Prerequisites

- `OPENAI_API_KEY`

Tip: in your shell, export the key before running:

```bash
export OPENAI_API_KEY=...
```

## Run

```bash
pnpm install
pnpm start
```
